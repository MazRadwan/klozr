Build a lightweight CRM web application using Next.js 15, Tailwind CSS 4, TypeScript, and Shadcn UI with Radix UI primitives, following a modular, scalable architecture. The application should include the following user flow and features, optimized for performance, accessibility (WCAG 2.1), and responsive design:

1. **Login Screen**:
   - Implement a secure login page with email/password authentication using NextAuth.js (v5) with credentials provider.
   - Include a "Remember Me" checkbox and a "Forgot Password" link (stubbed, no backend logic).
   - Use Shadcn UI’s Form component with Zod for validation (email format, password min 8 characters).
   - Style with Tailwind CSS 4, ensuring a clean, minimal design with high-contrast text and focus states.

2. **Dashboard**:
   - Create a dashboard as the landing page after login, displaying:
     - Summary cards for total customers, open deals, and recent activity (mock data).
     - Quick action buttons for "Add Customer" and "Create Deal" (linking to respective forms).
   - Use a fixed sidebar navigation (responsive hamburger menu on mobile) with links to Customer Database, Sales Pipeline, and Logout.
   - Implement a card-based layout with Shadcn UI’s Card component, animated with Framer Motion for subtle entrance effects.

3. **Customer Database**:
   - Build a searchable, filterable table of customers using Shadcn UI’s Data Table component.
   - Table columns: Name, Email, Phone, Status (Active/Inactive), with clickable rows linking to a customer details page.
   - Include a search bar with autocomplete and filters for status, powered by TanStack React Query for data fetching (mock data in JSON).
   - Support bulk actions (e.g., export, tag) via a dropdown menu.
   - Ensure pagination and accessibility (ARIA labels, keyboard navigation).

4. **Sales Pipeline**:
   - Implement a Kanban board using react-beautiful-dnd for drag-and-drop functionality, with stages: Lead, Qualification, Proposal, Closed.
   - Each deal card shows title, amount, and expected close date, styled with Shadcn UI’s Card component.
   - Include filters by stage and search functionality, with a toggle for Kanban or table view.
   - Use Framer Motion for smooth drag-and-drop animations and loading states.

**Technical Requirements**:
- Use Next.js 15 App Router for routing and server components where possible to optimize performance.
- Store mock data in a JSON file (customers.json, deals.json) under `/src/data`, with schemas:
  - Customers: { id: string, name: string, email: string, phone: string, status: "Active" | "Inactive", createdAt: string }
  - Deals: { id: string, title: string, amount: number, stage: string, probability: number, closeDate: string, customerId: string }
- Implement a Drizzle ORM with SQLite (better-sqlite3) for local development, with migrations for the above schemas.
- Use TanStack React Query for data fetching and caching, ensuring optimistic updates for drag-and-drop in the Sales Pipeline.
- Follow TypeScript best practices with strict typing, custom error types, and Zod for schema validation.
- Style with Tailwind CSS 4 and Shadcn UI, ensuring responsive design (mobile-first) and dark mode support.
- Add Framer Motion for subtle animations (e.g., card entrances, Kanban drag transitions).
- Ensure accessibility with ARIA labels, keyboard navigation, and Tailwind’s accessible color palette.
- Structure the project with:
  - `/src/app`: Next.js pages and layouts
  - `/src/components`: Reusable Shadcn UI components and custom components
  - `/src/lib`: Utilities, Drizzle setup, and mock data
  - `/src/types`: TypeScript interfaces and types
- Include basic unit tests for key components (e.g., Login Form, Data Table) using Jest and React Testing Library.

**Windsurf AI Rules**:
- Prioritize simplicity: avoid unnecessary dependencies and code duplication.
- Use functional components and hooks, no classes.
- Maintain a clean file structure with descriptive names (e.g., `CustomerTable.tsx`, `KanbanBoard.tsx`).
- Generate well-documented code with JSDoc comments for functions and components.
- Optimize for performance: use server components, lazy loading, and tree-shaking.
- Handle errors gracefully with custom error types and user-friendly messages.

**Output**:
- Generate the full project structure, including all necessary files (e.g., `app/layout.tsx`, `app/login/page.tsx`, `app/dashboard/page.tsx`, `components/CustomerTable.tsx`, `lib/db.ts`).
- Use Cascade’s Write Mode to create and test the project autonomously, running scripts and debugging as needed.
- Provide a `README.md` with setup instructions, including `npm install`, `npm run dev`, and database migration steps.
- Export the project as a GitHub repository template with a clean commit history.

Prompt Windsurf to start the project by generating the Next.js 15 boilerplate, configuring Tailwind CSS 4, Shadcn UI, and Drizzle ORM, then iteratively build each feature using Cascade’s multi-file editing and context awareness. Use Supercomplete to predict and generate TypeScript interfaces and component logic. Test the application locally and ensure it runs at `http://localhost:3000`.